# Project memory

## Merge and deployment preference

The user requested automatic merging for this project on September 19, 2026.

- For authorized work in `tkautz/tkautz.github.io`, carry the pull request through merge into `master` once the required checks pass and any blocking review findings are resolved. Do not ask for a separate merge confirmation.
- This applies to pull requests created or updated for the current task, not unrelated pull requests. Honor an explicit request to leave a draft, stop for review, or avoid deployment.
- A merge to `master` automatically deploys to GitHub Pages at https://timkautz.org/. Verify the deployment and the affected live pages before reporting completion.
- Keep the user's existing requirement to get approval before large refactors, and preserve original data and publication PDFs.
